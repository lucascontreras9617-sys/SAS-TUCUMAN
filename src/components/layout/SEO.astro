---
interface Props {
  title: string;
  description?: string;
  image?: string;
}

const { 
  title, 
  description = "Constituí tu Sociedad por Acciones Simplificada (SAS) de forma rápida, 100% digital y con respaldo legal de primer nivel en Argentina.", 
  image = "/og-image.jpg" 
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

const schemaData = {
  "@context": "https://schema.org",
  "@type": "LegalService",
  "name": "Estudio SAS - Legal-Tech",
  "image": new URL("/logo.jpg", Astro.site).toString(),
  "description": description,
  "address": {
    "@type": "PostalAddress",
    "addressLocality": "Córdoba / Mendoza",
    "addressRegion": "Argentina",
    "addressCountry": "AR"
  },
  "url": Astro.site.toString(),
  "telephone": "+5493814011766",
  "priceRange": "$$"
};
---
<meta name="description" content={description} />
<link rel="canonical" href={canonicalURL} />

<!-- Open Graph / Facebook -->
<meta property="og:type" content="website" />
<meta property="og:url" content={canonicalURL} />
<meta property="og:title" content={`${title} | Estudio SAS`} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image, Astro.site)} />

<!-- Twitter -->
<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={canonicalURL} />
<meta property="twitter:title" content={`${title} | Estudio SAS`} />
<meta property="twitter:description" content={description} />
<meta property="twitter:image" content={new URL(image, Astro.site)} />

<!-- JSON-LD Structured Data -->
<script type="application/ld+json" set:html={JSON.stringify(schemaData)} />
